apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "linview-oss.fullname" . }}-test-connection"
  labels:
    {{- include "linview-oss.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "linview-oss.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
